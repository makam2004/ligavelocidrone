services:
  - type: web
    name: velocidrone-app
    env: python
    buildCommand: |
      apt-get update && \
      apt-get install -y chromium && \
      pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
